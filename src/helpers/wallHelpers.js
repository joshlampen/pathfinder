// export default function makeWall(grid, col, row) {
//   const gridCopy = grid.slice();
//   const node = newGrid[row][col];
//   const newNode = { ...node, isWall: !node.isWall };
//   gridCopy = newNode;

//   return gridCopy;
// }