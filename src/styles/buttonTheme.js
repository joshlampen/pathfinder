import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1167b1',
      dark: '#03254c',
      contrastText: '#fff'
    },
    secondary: {
      main: '#11b15b',
      dark: '#0e8c55',
      contrastText: '#fff'
    }
  }
})